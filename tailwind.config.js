/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './templates/**/*.html',    // Rutas a tus archivos HTML en la carpeta templates
    './static/**/*.css',        // Rutas a tus archivos CSS en la carpeta static
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

